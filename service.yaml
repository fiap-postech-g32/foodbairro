apiVersion: v1
kind: Service
metadata:
  name: foodbairro-app-service
spec:
  selector:
    app: foodbairro-app
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 3000
  type: LoadBalancer
