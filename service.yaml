apiVersion: v1
kind: Service
metadata:
  name: foodbairro-service
spec:
  type: NodePort
  selector:
    app: foodbairro
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30001
